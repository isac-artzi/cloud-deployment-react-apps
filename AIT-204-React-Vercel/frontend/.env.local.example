# ============================================================================
# Frontend Environment Variables (Development)
# ============================================================================
#
# This file contains example environment variables for local development.
# Copy this file to '.env.local' and update with your actual values.
#
# Command: cp .env.local.example .env.local
#
# IMPORTANT: In Vite, only variables prefixed with VITE_ are exposed to the app
# ============================================================================

# Backend API URL
# ---------------
# For local development, this should point to your local FastAPI server
VITE_API_URL=http://localhost:8000

# Application Configuration
# -------------------------
VITE_APP_NAME="AI Image Classifier"
VITE_APP_VERSION="1.0.0"

# Feature Flags (optional)
# ------------------------
VITE_ENABLE_DEBUG=true
VITE_ENABLE_ANALYTICS=false

# ============================================================================
# Notes:
# ============================================================================
#
# 1. File Naming:
#    - .env.local: Local development (not committed to git)
#    - .env.production: Production build
#    - .env: Default for all environments
#
# 2. Vite Environment Variables:
#    - Must start with VITE_ to be exposed
#    - Access in code: import.meta.env.VITE_API_URL
#    - Built into the client bundle
#
# 3. Security:
#    - Never commit .env.local to git
#    - Don't store secrets in VITE_ variables
#    - They're exposed in the browser!
#
# ============================================================================
